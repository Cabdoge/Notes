\documentclass{sjtureport}

\usepackage{amsmath,amsthm}
\usepackage{bm}
\usepackage{physics}
\usepackage{ytableau}
\usepackage{imakeidx}
\usepackage{longtable}
\makeindex[title=关键词索引, columns=2, columnsep=1.5cm, intoc, options=-s pinyin.ist]

\usepackage[hidelinks]{hyperref}

%================ 页面布局 ================
\usepackage{geometry}

\geometry{
    %left=3cm,
    %right=3cm,
    %top=3.54cm,
    bottom=3cm,
}

%================ 行距 ================
\usepackage{setspace}
\setstretch{1.3}

\usepackage{calc}
\setlength{\lineskip}{\baselineskip-\ccwd}
\setlength{\lineskiplimit}{2.5pt}

\newtheorem{theorem}{定理}[chapter]
\newtheorem{lemma}[theorem]{引理}
\newtheorem{proposition}[theorem]{命题}
\newtheorem{corollary}[theorem]{推论}
\newtheorem{definition}{定义}[chapter]
\newtheorem{example}{例}[chapter]
\newtheorem{remark}{注}[chapter]
\newenvironment{solution}{\begin{proof}[\bf 解]}{\end{proof}}
\renewcommand{\proofname}{\bf 证明}
%\renewcommand{\qedsymbol}{$\blacksquare$}

%================ 缩小公式上下间距 ================
\AtBeginDocument{
  \abovedisplayskip=5pt plus 1pt minus 1.5pt
  \belowdisplayskip=5pt plus 1pt minus 1.5pt
  \abovedisplayshortskip=0pt plus 3pt
  \belowdisplayshortskip=4pt plus 1pt minus 1.5pt 
}

\renewcommand{\arraystretch}{1}
\newcommand{\tabloid}[1]{
    \renewcommand{\arraystretch}{1.1} % 稍微撑开一点行距，比较美观
    \begin{array}{l}
        \hline
        #1 \\
        \hline
    \end{array}
}

\everymath{\displaystyle}

\DeclareMathOperator{\sh}{sh}

\title{对称群的表示与特征标理论}
\author{庞逸天}
\subject{群与代数表示论期中大作业}
\keywords{上海交大,饮水思源,爱国荣校}

\begin{document}
\maketitle

\tableofcontents

\chapter*{符号说明}

{\small
\setlength{\tabcolsep}{6pt}
\renewcommand{\arraystretch}{1.2}
\begin{longtable}{p{3.2cm}p{\dimexpr\linewidth-3.4cm-4\tabcolsep\relax}}
    \hline
    \textbf{符号} & \textbf{说明} \\
    \hline
    \endfirsthead
    \hline
    \textbf{符号}（续） & \textbf{说明}（续） \\
    \hline
    \endhead
    \hline
    \endfoot
    \multicolumn{2}{c}{\textit{组合基础}} \\ \hline
    $\lambda \dashv n$ & $\lambda$是$n$的一个分拆 \\
    $\rho(n)$ & $n$的分拆的个数 \\
    $\lambda\rhd\mu$ & $\lambda$支配$\mu$（支配序） \\
    $\sh t$ & Young表$t$的形状 \\
    $f^\lambda$ & 形状为$\lambda$的标准Young表个数 \\
    $h_{i,j}$ & Young图方格$(i,j)$的钩长 \\
    $H(\lambda)$ & $\lambda$的全图钩长之积$\prod h_{i,j}$ \\
    $C(\lambda)$ & $\lambda$的可移角格集合 \\ \hline
    \multicolumn{2}{c}{\textit{群与代数}} \\ \hline
    $S_n$ & $n$阶对称群 \\
    $C_\mu$ & 轮换类型为$\mu$的共轭类 \\
    $z_\mu$ & 共轭类$C_\mu$的中心化子的阶 \\
    $R_t$ & Young表$t$的行群 \\
    $C_t$ & Young表$t$的列群 \\
    $a_t$ & Young表$t$的行对称化子 \\
    $b_t$ & Young表$t$的列反对称化子 \\
    $c_t$ & Young对称化子$c_t = a_t b_t$ \\
    $G_{A,B}$ & Garnir元 \\
    $\mathbb{C}[S_n]$ & $S_n$的群代数 \\
    $C(S_n)$ & $S_n$的类函数空间 \\
    $[\chi,\psi]$ & 类函数内积$\frac{1}{n!}\sum_\sigma\chi(\sigma)\overline{\psi(\sigma)}$ \\ \hline
    \multicolumn{2}{c}{\textit{表示与模}} \\ \hline
    $\{t\}$ & Young表$t$的行等价报（tabloid） \\
    $\mathrm{span}$ & 张成的线性子空间 \\
    $M^\lambda$ & 分拆$\lambda$对应的置换模 \\
    $e_t$ & Young表$t$的多项表（polytabloid） \\
    $S^\lambda$ & 分拆$\lambda$对应的Specht模 \\
    $\dim S^\lambda$ & Specht模的维数（$=f^\lambda$） \\
    $\langle\cdot,\cdot\rangle$ & $M^\lambda$上的$S_n$-不变内积 \\
    $\chi^\lambda_\mu$ & $S^\lambda$在共轭类$C_\mu$上的不可约特征标值 \\ \hline
    \multicolumn{2}{c}{\textit{对称多项式}} \\ \hline
    $p_k,\,p_\mu$ & 幂和对称多项式 \\
    $s_\lambda$ & Schur多项式 \\
    $a_\alpha$ & 交错和$a_\alpha=\det(x_i^{\alpha_j})$ \\
    $\Delta(x)$ & Vandermonde行列式$\prod_{i<j}(x_i-x_j)$ \\
    $\delta$ & 阶梯向量$(n-1,n-2,\dots,1,0)$ \\
    $\Lambda_n$ & $n$次对称多项式环 \\
    $\mathcal{F}$ & Frobenius特征标映射$\mathcal{F}:C(S_n)\to\Lambda_n$ \\
    $\langle p_\mu,p_\nu\rangle$ & Hall内积（$=\delta_{\mu\nu}z_\mu$） \\
    $\Phi_\lambda(x)$ & Hook-content有理函数 \\ \hline
    \multicolumn{2}{c}{\textit{边界带}} \\ \hline
    $\xi$ & 边界带（border strip） \\
    $\operatorname{ht}(\xi)$ & 边界带$\xi$的高度（leg length） \\
    $\varepsilon(\xi)$ & 边界带$\xi$的符号$(-1)^{\operatorname{ht}(\xi)}$ \\
    $\lambda\setminus\xi$ & 从$\lambda$移去边界带$\xi$后得到的分拆 \\
    $T$ & 边界带表（border strip tableau） \\
    $\operatorname{ht}(T)$ & 边界带表$T$的总高度 \\
    $\varepsilon(T)$ & 边界带表$T$的符号$(-1)^{\operatorname{ht}(T)}$ \\ \hline
\end{longtable}
}


\input{Young表.tex}


\chapter{对称群的不可约表示}

本章将使用Young表和Young对称化子来构造对称群$S_n$的不可约表示，称为Specht模.通过分析Specht模的结构，我们可以证明它们确实给出了$S_n$的所有不可约表示.但由于直接分析最终的结论较为复杂，本章将依次施加对称性以达到最终目的.

\section{置换模}


首先，我们只考虑行对称的情况，为此需要定义行等价.

\begin{definition}[行等价]\index{h@行等价}
    设$\sh t_1=\sh t_2=\lambda\dashv n$，则称Young表$t_1,t_2$是行等价的，当且仅当它们的每一行拥有相同的元素，记作$t_1\sim t_2$.
\end{definition}

在等价关系下，可以研究其等价类.
\begin{definition}[行等价报]\index{h@行等价报 (tabloid)}
    记$\{t\}=\{t_1|t_1\sim t\}$为所有行等价Young表的等价类，称其为行等价报或$\lambda-\!$报，其中$\sh t=\lambda\dashv n$.
\end{definition}

\begin{example}
    若$t=\ytableaushort{12,3}$，则
    \begin{equation*}
        \{t\} = \qty{\ytableaushort{12,3},\ytableaushort{21,3}}=\begin{array}{ll}
            \hline
            1 & 2 \\
            \hline
            3 \\
            \cline{1-1}
        \end{array}
    \end{equation*}
\end{example}

\begin{lemma}[行等价报中元素个数]
    若$\sh t=\lambda=(\lambda_1,\ldots,\lambda_k)\dashv n$，则$\{t\}$中的元素个数为
    \begin{equation*}
        \lambda !\triangleq \lambda_1!\lambda_2!\cdots\lambda_k!.
    \end{equation*}
    特别地，$\lambda-\!$报的个数为$\dfrac{n!}{\lambda!}$
\end{lemma}
\begin{proof}
    对于形状为$\lambda=(\lambda_1,\ldots,\lambda_k)$的Young表$t$，其第$i$行含有$\lambda_i$个元素，对这些元素在其所在行内进行全排列共有$\lambda_i!$种方式. 由于各行的排列相互独立，因此与$t$行等价的Young表总数为
    \begin{equation*}
        \lambda_1!\lambda_2!\cdots\lambda_k! = \lambda!.
    \end{equation*}
    
    另一方面，形状为$\lambda$的Young图共有$n!$种不同的填法（即共有$n!$个形状为$\lambda$的Young表），并且$\lambda-\!$报中恰好包含$\lambda!$个元素. 因此$\lambda-\!$报的总数为
    \begin{equation*}
        \frac{n!}{\lambda!}.
    \end{equation*}
\end{proof}


\begin{definition}[置换模]\index{z@置换模 $M^\lambda$}
    设$\lambda\dashv n$，定义对应于$\lambda$的置换模为
    \begin{equation*}
        M^\lambda\triangleq \mathbb{C}\qty{{t_1},{t_2},\cdots,{t_k}},
    \end{equation*}
    其中，$\{t_1\},\ldots,\{t_k\}$是所有形状为$\lambda$的$\lambda-\!$报.
\end{definition}

由于我们可以自然地将$S_n$作用在$\{t\}$上，因此置换模实际上是$S_n-\!$模. 

从表示论的角度来看，置换模本质上是一个诱导表示. 由于行群$R_t$中的元素保持$\{t\}$不变，它在生成元$\{t\}$上的作用是平凡的. $S_n$在$\lambda-\!$报的集合上进行传递置换作用，其稳定子群正好是$R_t$.\par 
因此，置换模$M^\lambda$同构于行群$R_t$的平凡表示诱导到$S_n$上的表示，即
\begin{equation*}
    M^\lambda \cong \mathrm{Ind}_{R_t}^{S_n}(1).
\end{equation*}

\begin{example}
    \begin{enumerate}[label=(\arabic*)]
        \item 若$\lambda=(n)$，则
        \begin{equation*}
            M^{(n)}=\mathbb{C}\qty{\begin{array}{l}
            \hline
            12\cdots n\\
            \hline\end{array}},
        \end{equation*}
        它是$S_n$的平凡表示.
        \item 若$\lambda=(1,1,\ldots,1)$，则
        \begin{equation*}
            M^{(1,1,\ldots,1)}=\mathbb{C}\qty{\begin{array}{l}
            \hline
            1\\
            \hline
            2\\
            \hline
            \vdots\\
            \hline
            n\\
            \hline\end{array}}\cong \mathbb{C}{S_n},
        \end{equation*}
        它是$S_n$的正则表示.
    \end{enumerate}
        
\end{example}


\section{Specht模}
$M^\lambda$仍然可约，甚至于可以是正则表示包含所有的不可约表示，这表明该空间依旧较大.为此，我们可以在$\lambda-\!$报的基础上施加列反对称性，在这个空间中切出较小的一块.

\begin{definition}[多项表]\index{d@多项表 $e_t$}
    设$t$是一个Young表，则其多项表定义为列反对称化子作用在$\{t\}$上的结果，即
    \begin{equation*}
    e_t = b_t \{t\} = \sum_{\tau \in C_t} \mathrm{sgn}(\tau)\tau \{t\}.
    \end{equation*}    
\end{definition}

\begin{definition}[Specht模]\index{s@Specht 模 $S^\lambda$}
    设$\lambda\dashv n$，则定义Specht模$S^\lambda$为
    \begin{equation*}
        S^\lambda = \mathrm{span}_\mathbb{C}\{e_t \mid \sh t = \lambda\}.
    \end{equation*}
\end{definition}

另外，从群代数$\mathbb{C}[S_n]$的角度来看，Specht模也可以等价地看作是由群代数中生成的左理想，即其等价定义为：
\begin{theorem}[Specht模的等价定义]\index{s@Specht 模 $S^\lambda$}
    Specht模$S^\lambda$同构于由$c_t$生成的群代数$\mathbb{C}[S_n]$中的左理想：
\begin{equation*}
    S^\lambda \cong \mathbb{C}[S_n]c_t.
\end{equation*}
\end{theorem}
\begin{proof}
    定义$\mathbb{C}[S_n]-\!$模同态
    \begin{equation*}
        \begin{aligned}
            \varphi: \mathbb{C}[S_n] &\to M^\lambda \\[-5pt]
            \quad x &\mapsto x\{t\}.
        \end{aligned}
    \end{equation*}
    显然$\varphi$是满射. 对于行对称化子$a_t=\sum_{\pi\in R_t}\pi$，由于$R_t$中所有元素均保持$\{t\}$不变，因此在$M^\lambda$中有$a_t\{t\} = |R_t|\{t\}$. 考虑群代数中的左理想$\mathbb{C}[S_n]a_t$，容易验证$\varphi$对其限制给出了$\mathbb{C}[S_n]a_t$与$M^\lambda$之间的$\mathbb{C}[S_n]-\!$模同构，且有基本对应关系$\{t\} \leftrightarrow \frac{1}{|R_t|}a_t$. 
    
    在此同构映射下，多项表$e_t$的像满足：
    \begin{equation*}
        e_t = b_t\{t\} \longleftrightarrow b_t\qty(\frac{1}{|R_t|}a_t) = \frac{1}{|R_t|}b_t a_t = \frac{1}{|R_t|}c_t.
    \end{equation*}
    由于Specht模$S^\lambda$作为$M^\lambda$的子模是由多项表$e_t$生成的，即$S^\lambda = \mathbb{C}[S_n]e_t$，因此在上述同构下自然有$S^\lambda \cong \mathbb{C}[S_n]c_t$.
\end{proof}



\begin{theorem}
    对于每一个分拆$\lambda \dashv n$，Specht模$S^\lambda$给出了对称群$S_n$的一个不可约表示. 且不同的分拆给出同构意义下不同的不可约表示.
\end{theorem}


\section{$M^\lambda$和$S^\lambda$的结构与性质}

本节将聚焦于分析置换模和Specht模的结构，为最终证明Specht模的不可约性和不同分拆对应不同不可约表示做准备.

\begin{lemma}[$M^\lambda$上的内积]\index{n@内积（$S_n$-不变内积）}
    定义$M^\lambda$上的内积为
    \begin{equation*}
        \langle \{t_1\},\{t_2\}\rangle=\delta_{\{t_1\},\{t_2\}}=\begin{cases}
            1, &\{t_1\}=\{t_2\},\\
            0, &\{t_1\}\neq \{t_2\}.
        \end{cases}
    \end{equation*}
    并且该内积是$S_n-\!$不变的.
\end{lemma}
\begin{proof}
    由于$M^\lambda$是以所有的$\lambda-\!$报作为基张成的向量空间，上述等式实际上是将这组基定义为标准正交基，并共轭双线性地扩展到整个$M^\lambda$空间上，这自然满足内积的正定性、对称性等要求. 
    
    此外，对任意$\pi \in S_n$，$\pi\{t_1\}=\pi\{t_2\} \iff \{t_1\}=\{t_2\}$，因此对于任意基底有：
    \begin{equation*}
        \langle \pi \{t_1\},\pi \{t_2\}\rangle = \delta_{\pi \{t_1\},\pi \{t_2\}} = \delta_{\{t_1\},\{t_2\}} = \langle \{t_1\},\{t_2\}\rangle.
    \end{equation*}
    这表明群$S_n$的表示作用保持内积不变，即该内积为$S_n-\!$不变内积.
\end{proof}

更特别地，该内积有以下伴随关系和自伴随关系：
\begin{lemma}[伴随关系]
    对于任意$\pi\in S_n$，有以下伴随关系：
    \begin{equation*}
        \langle \pi\{t_1\}, \{t_2\}\rangle = \langle \{t_1\},\pi^{-1}\{t_2\}\rangle.
    \end{equation*}
    特别地，列反对称化子是自伴的，即
    \begin{equation*}
        \langle b_t\{t_1\}, \{t_2\}\rangle = \langle \{t_1\},b_t\{t_2\}\rangle.
    \end{equation*}
\end{lemma}
\begin{proof}
    由内积的$S_n-\!$不变性可直接得到第一式：
    \begin{equation*}
        \langle \pi\{t_1\}, \{t_2\}\rangle = \langle \pi^{-1}(\pi\{t_1\}), \pi^{-1}\{t_2\}\rangle = \langle \{t_1\}, \pi^{-1}\{t_2\}\rangle.
    \end{equation*}
    
    对于列反对称化子$b_t = \sum_{\tau \in C_t} \mathrm{sgn}(\tau)\tau$，利用上述伴随关系可得：
    \begin{equation*}
        \langle b_t\{t_1\}, \{t_2\}\rangle = \sum_{\tau \in C_t} \mathrm{sgn}(\tau) \langle \tau\{t_1\}, \{t_2\}\rangle = \sum_{\tau \in C_t} \mathrm{sgn}(\tau) \langle \{t_1\}, \tau^{-1}\{t_2\}\rangle.
    \end{equation*}
    由于当$\tau$遍历$C_t$时，$\tau^{-1}$也恰好遍历$C_t$，且$\mathrm{sgn}(\tau) = \mathrm{sgn}(\tau^{-1})$，因此
    \begin{equation*}
        \sum_{\tau \in C_t} \mathrm{sgn}(\tau) \langle \{t_1\}, \tau^{-1}\{t_2\}\rangle = \langle \{t_1\}, \sum_{\tau \in C_t} \mathrm{sgn}(\tau^{-1})\tau^{-1}\{t_2\}\rangle = \langle \{t_1\}, b_t\{t_2\}\rangle. \qedhere
    \end{equation*}
\end{proof}

下一个引理是纯组合学上的，它连接了杨表的“形状”与群代数的“计算”.

\begin{lemma}[多项表的极值性]{\label{lem:extremality}}\index{d@多项表极值性（择一性）}
    设$\sh t=\lambda\dashv n$，$\{t'\}\in M^\lambda$，考虑列反对称化子$b_t$在$\{t'\}$上的作用，则有以下择一性：
    \begin{enumerate}
        \item 若$t$中有两个在同一列的数字出现在$t'$的同一行中，则$b_t\{t'\} = 0$；
        \item 否则，$b_t\{t'\} =\pm e_t$.
    \end{enumerate}
\end{lemma}
\begin{proof}
    (1) 设$x,y$是$t$中同一列的两个数字，且它们同时出现在$t'$的同一行中.\par
    考虑对换$(x,y)$，由于$x,y$在$t$的同一列，有$(x,y) \in C_t$. 又由于$x,y$在$t'$的同一行，故$(x,y)\{t'\} = \{t'\}$. \par
    考虑子群$\{\mathrm{id}, (x,y)\}$对列群$C_t$作左陪集分解，记其左陪集代表元系为$H$，则$C_t = H \cup H(x,y)$. 此时，
    \begin{equation*}
        b_t\{t'\} = \sum_{\tau \in H} \qty[ \mathrm{sgn}(\tau)\tau \{t'\} + \mathrm{sgn}(\tau(x,y))\tau(x,y) \{t'\} ].
    \end{equation*}
    因为$\mathrm{sgn}(\tau(x,y)) = -\mathrm{sgn}(\tau)$且$(x,y)\{t'\}=\{t'\}$，所以方括号内每一项均为0，从而$b_t\{t'\} = 0$.
    
    (2) 假设条件(1)不成立，即$t'$的任一行中没有任何两个数字属于$t$的同一列. 这意味着$t'$第一行的各数字必然分属$t$的不同列.\par
    由于$t'$与$t$具有相同的形状$\lambda$，必然存在一个列群中的置换$\tau_1 \in C_t$，把$t'$第一行的所有数字移入$t$的第一行.\par
    对剩下的行依次重复此推理，可知存在某个列置换$\tau \in C_t$，使得$\tau$将$t'$每一行的内容移到了$t$对应的行中，即满足$\tau\{t'\} = \{t\}$.
    因此，$\{t'\} = \tau^{-1}\{t\}$.\par
    进而有，
    \begin{equation*}
        b_t\{t'\} = b_t (\tau^{-1}\{t\}) = (b_t\tau^{-1})\{t\} = \mathrm{sgn}(\tau)b_t\{t\} = \mathrm{sgn}(\tau)e_t = \pm e_t. \qedhere
    \end{equation*}
\end{proof}

\begin{corollary}{\label{cor:extremality}}
    设$\lambda\dashv n$，则$\forall\, \{t\}\in M^\lambda$，存在复数$c\in \mathbb{C}$，使得$b_t\{t\}=c \cdot e_t$.
\end{corollary}
\begin{proof}
    对$M^\lambda$中的任意元素$\{t\}$，其可写作$\{t\}=c_1\{t_1\} + c_2\{t_2\} + \cdots + c_k\{t_k\}$，其中$\{t_1\}$为$\sh t_i=\lambda$的互不等价的$\lambda-\!$报.\par
    由$b_t$的线性线性质与引理\ref{lem:extremality}，
    \begin{equation*}
        b_t\{t\} = c_1 b_t\{t_1\} + c_2 b_t\{t_2\} + \cdots + c_k b_t\{t_k\}=c_1' e_t + c_2' e_t + \cdots + c_k' e_t 
    \end{equation*}
    其中$c_i'\in {-1,0,1}$，故推论自然成立.
\end{proof}

\section{子模定理和Specht模的不可约性}

\begin{theorem}[子模定理]\index{z@子模定理}
    设$U$为$M^\lambda$的子模，对于任意的$\lambda \dashv n$，要么$S^\lambda \subseteq U$，要么$U\subseteq \qty(S^\lambda)^{\perp}$.
\end{theorem}
\begin{proof}
    假设$U \not\subseteq (S^\lambda)^\perp$，我们需要证明这必然导致$S^\lambda \subseteq U$.
    
    因为$U$不在$S^\lambda$的正交补中，所以必定存在某个$u \in U$和某个多项表$e_t \in S^\lambda$，使得它们的内积不为零：$\langle u, e_t \rangle \neq 0$. 
    
    展开$e_t = b_t\{t\}$，并利用列反对称化子$b_t$的自伴随性质，可得：
    \begin{equation*}
        0 \neq \langle u, e_t \rangle = \langle u, b_t\{t\} \rangle = \langle b_t u, \{t\} \rangle.
    \end{equation*}
    因此向量$b_tu$不是零向量.
    
    由推论{\ref{cor:extremality}}，$b_tu = c \cdot e_t$. 由于它不是零向量，那么标量$c \neq 0$.
    
    回到代数结构：因为$u \in U$，且$U$是一个$S_n-\!$子模，而$b_t \in \mathbb{C}[S_n]$，所以$b_tu \in U$.
    
    既然$b_tu \in U$且$b_tu = c \cdot e_t$（其中$c \neq 0$），这意味着$e_t \in U$.
    
    那么对所有$\pi \in S_n$均有$\pi e_t \in U$，即所有的多项表都被包含在$U$中. \par
    因$S^\lambda$由所有多项表张成，这就意味着整个Specht模必定包含于$U$，从而必然有$S^\lambda \subseteq U$.
\end{proof}

我们终于可以介绍本章的核心定理.
\begin{theorem}[Specht模的不可约性]\index{s@Specht 模的不可约性}
    对于任意整数分拆$\lambda \dashv n$，Specht模$S^\lambda$是对称群$S_n$的一个不可约表示.
\end{theorem}
\begin{proof}
    设$W$是$S^\lambda$的任意一个子模. 我们需要证明$W=\{0\}$或$W=S^\lambda$.
    
    因为$W \subseteq S^\lambda \subset M^\lambda$，$W$自然也是$M^\lambda$的子模. 
    对$W$应用子模定理，只有两种可能：
    \begin{itemize}
        \item \textbf{情况 A}：$S^\lambda \subseteq W$. 既然$W$本来就是$S^\lambda$的子集，这直接意味着$W = S^\lambda$.
        \item \textbf{情况 B}：$W \subseteq (S^\lambda)^\perp$. 这同时意味着$W$里面的向量既在$S^\lambda$里，又在$S^\lambda$的正交补里.\par
        在复数域上，内积是正定的，一个空间与自己正交补的交集只有零向量，即
    \begin{equation*}
        W \subseteq S^\lambda \cap (S^\lambda)^\perp = \{0\}.
    \end{equation*}
    因此，$W = \{0\}$.
    \end{itemize}
    
    
    
    综上所述，$S^\lambda$除了自身和零空间外，没有非平凡子模. 故$S^\lambda$是不可约的.
    
    接下来证明不同的分拆对应不同的不可约表示. 假设存在不同的分拆 $\lambda \neq \mu$ 使得 $S^\lambda \cong S^\mu$. 不失一般性，假设在字典序（或优势序）下 $\lambda \not\unrhd \mu$. 
    对于形状为 $\lambda$ 的 Young 表 $t$，由于 $t'$ 是形状为 $\mu$ 的 Young 表，根据 $\lambda \not\unrhd \mu$ 的组合性质，$t$ 中必然存在两个处于同一列的元素，它们出现在 $t'$ 的同一行中. 
    由多项表的极值性引理可知，列反对称化子 $b_t$ 对 $M^\mu$ 中任一基元素作用均为 $0$，即 $b_t M^\mu = 0$. 由于 $S^\mu \subseteq M^\mu$，故 $b_t S^\mu = 0$.
    另一方面，在 $S^\lambda$ 中，有 $b_t e_t = |C_t| e_t \neq 0$. 
    若存在同构映射 $\phi: S^\lambda \to S^\mu$，则：
    \begin{equation*}
        0 \neq \phi(|C_t| e_t) = \phi(b_t e_t) = b_t \phi(e_t) \in b_t S^\mu = 0,
    \end{equation*}
    得出矛盾. 因此 $S^\lambda$ 与 $S^\mu$ 不可能同构.
\end{proof}

但目前还未知这样的$S^\lambda$是否是一一的，即是否存在两个不同的分拆会得到相同的不可约表示，为了需要引入支配序


\begin{definition}[支配序]\index{z@支配序 $\lambda\rhd\mu$}
    设$\lambda,\mu\dashv n$，如果对于任意的正整数$k\geq 1$，满足：
    \begin{equation*}
        \sum_{i=1}^k\lambda_i\geq \sum_{i=1}^k\mu_i
    \end{equation*}
    其中，若$\lambda_j,\mu_j$不存在，则视为0，那么称$\lambda$支配$\mu$，记作$\lambda \rhd \mu$.
\end{definition}
支配序是分拆之间的一种偏序关系，在直观上若$\lambda\rhd \mu$，那么$\lambda$的Young图会比$\mu$的Young图更扁更短.但需要注意，存在两个分拆是不可比的，例如$(3,3)$和$(4,1,1)$.

在支配序的基础上，可以证明以下重要的引理.

\begin{lemma}[]
    设$\lambda,\mu\dashv n$，若存在非零的$S_n-\!\!$模同态$\phi:S^\lambda\to M^\mu$，则$\lambda\rhd \mu$.
\end{lemma}
\begin{proof}
    由于 $\phi$ 是非零同态，且 $S^\lambda$ 由多项表 $e_t$ 生成，必然存在某个形状为 $\lambda$ 的Young表 $t$，使得 $\phi(e_t) \neq 0$.
    
    利用多项表的定义 $e_t = b_t\{t\}$ 以及 $\phi$ 是 $S_n-\!$模同态，我们有：
    \begin{equation*}
        0 \neq \phi(e_t) = \phi(b_t\{t\}) = b_t\phi(\{t\}).
    \end{equation*}
    
    我们知道 $\phi(\{t\})$ 是置换模 $M^\mu$ 中的元素，因此它可以写成$\mu-\!\!$报$\{s\}$ 的线性组合：
    \begin{equation*}
        \phi(\{t\}) = \sum c_s\{s\}.
    \end{equation*}
    
    既然 $b_t\phi(\{t\}) \neq 0$，那么展开后，必然存在至少一个$\mu-\!\!$报$\{s\}$，使得 $b_t\{s\} \neq 0$.
    
    由引理\ref{lem:extremality}，如果 $b_t\{s\} \neq 0$，那么 $t$ 中位于同一列的任意两个数字，绝不可能出现在 $s$ 的同一行中.
    
    下面利用抽屉原理进行证明：考察 $s$ 的第1行，它有 $\mu_1$ 个元素. 根据上述结论，这 $\mu_1$ 个元素必须来自 $t$ 的不同列. 由于 $t$ 总共只有 $\lambda_1$ 列，所以必然有 $\mu_1 \leq \lambda_1$.
    
    更一般地，考察 $s$ 的前 $k$ 行，共有 $\sum_{i=1}^k \mu_i$ 个元素. 它们在 $t$ 的每一列中最多只能各占 $1$ 个位置. 因此，前 $k$ 行的元素总数不能超过 $t$ 的前 $k$ 行所能容纳的元素总数，即
    \begin{equation*}
        \sum_{i=1}^k \mu_i \leq \sum_{i=1}^k \lambda_i.
    \end{equation*}
    
    这就严格证明了 $\lambda \rhd \mu$.
\end{proof}

最后，我们就可以证明不同分拆对应的不可约表示不同.

\begin{theorem}[]\index{b@不可约表示的分类}
    设$\lambda,\mu\dashv n$且$\lambda\neq \mu$，则$S^\lambda\ncong S^\mu$.
\end{theorem}
\begin{proof}
    应用反证法：假设存在$S_n-\!$模同构 $\theta: S^\lambda \xrightarrow{\sim} S^\mu$.
    
    因为 $S^\mu$是置换模 $M^\mu$ 的子模，所以 $\theta$ 自然也可以看作是从 $S^\lambda$ 到 $M^\mu$ 的非零同态.
    
    根据前述引理，这蕴含了 $\lambda \rhd \mu$.
    
    同理，既然 $\theta$ 是同构，它的逆映射 $\theta^{-1}: S^\mu \xrightarrow{\sim} S^\lambda$ 也是非零同态，自然也可看作是到 $M^\lambda$ 的非零同态，这蕴含了 $\mu \rhd \lambda$.
    
    支配序满足反对称性. 既然 $\lambda \rhd \mu$ 且 $\mu \rhd \lambda$，必然有 $\lambda = \mu$.
    
    这与 $\lambda \neq \mu$ 矛盾.
\end{proof}

最终，由于$S_n$的不可约表示的个数等于分拆的个数，且$S_n$的Specht模的个数等于分拆的个数，我们就可以得知每一个分拆$\lambda\dashv n$都对应着一个唯一的不可约表示$S^\lambda$，从而完成了对称群不可约表示的分类.

\section{维数定理}

本章的最后一个问题，既然我们已经找到了所有的$S_n$的不可约表示，为了方便特征标的计算，需要知道它们各自的维数.这等价于其对应的Specht模的维数.\par
我们知道Specht模是由形状为$\lambda$的多项表生成的，因此想研究其维数就需要研究形状为$\lambda$的多项表之间的关系.

\begin{definition}[$\lambda-\!$报的字典序]
    对于两个不同的$\lambda-\!$报 $\{t\}$ 和 $\{s\}$，我们比较元素 $1, 2, \dots, n$ 在它们中所在的行号.找到最大的那个所在行号不同的元素 $k$.如果 $k$ 在 $\{t\}$ 中的行号小于它在 $\{s\}$ 中的行号，则称$\{t\} \rhd \{s\}$
\end{definition}
一个观察是对于任何杨表 $t$，在多项表 $e_t = \sum_{\tau \in C_t} \mathrm{sgn}(\tau)\tau \{t\}$ 的所有展开项中，$\{t\}$ 本身总是唯一的序最大项.这是因为列群中的非平凡置换必然会把某些元素移到更低的行.


\begin{lemma}[标准多项表线性无关]
    所有标准杨表生成的多项表必然线性无关.
\end{lemma}
\begin{proof}
    假设存在一组互不相同的标准杨表 $t_1, t_2, \dots, t_m$ 和不全为零的常数 $c_i$，使得：
    \begin{equation*}
        \sum_{i=1}^m c_i e_{t_i} = 0.
    \end{equation*}
    在所有系数 $c_i \neq 0$ 的项中，选出使得$\lambda-\!$报 $\{t_k\}$ 在字典序下达到极大的一项.\par
    我们将和式展开为所有形式为 $\{t\}$ 的$\lambda-\!$报的线性组合. 对于等式左侧经过展开后，$\{t_k\}$ 的系数贡献，我们逐项进行分析：\par
    首先，由于 $e_{t_k} = \sum_{\tau \in C_{t_k}} \mathrm{sgn}(\tau)\tau \{t_k\}$，其中 $\{t_k\}$ 始终是 $e_{t_k}$ 展开式中唯一的序最大项.\par
    因此，在 $e_{t_k}$ 中，$\{t_k\}$ 的系数仅仅是 $1$（对应于 $\tau = \mathrm{id}$ 的情况）.\par
    其次，对于任意 $j \neq k$（且 $c_j \neq 0$），我们要说明 $e_{t_j}$ 的展开式中不可能出现 $\{t_k\}$：如果 $e_{t_j}$ 中包含 $\{t_k\}$ 项，则必然存在列群中的置换 $\tau \in C_{t_j}$ 使得 $\tau \{t_j\} = \{t_k\}$. \par
    根据对于字典序的观察，展开项的序总是小于等于原项，这要求 $\{t_k\} = \tau \{t_j\} \unlhd \{t_j\}$. 由于第一步选择 $\{t_k\}$ 时取的是所有非零项中的极大元，必须同时有 $\{t_j\} \unlhd \{t_k\}$，于是只有当 $\{t_k\} = \{t_j\}$ 时等号才成立.\par
    然而，$t_k$ 和 $t_j$ 都是标准杨表，它们的每一行内部必然已经全部按严格递增且从左向右排列. 如果 $\{t_k\} = \{t_j\}$，在它们同时满足升序排列的前提下，意味着必然有 $t_k = t_j$. 这与 $t_i$ 互不相同的假设直接矛盾.\par
    因此，对于所有的 $j \neq k$ ，$e_{t_j}$ 根本无法产生 $\{t_k\}$ 项来与 $c_k e_{t_k}$ 产生的 $\{t_k\}$ 相抵消. 这就导致了整个左边求和式展开后，$\{t_k\}$ 的总系数恰好是不等于0的 $c_k$.\par
    这与等式右边为 $0$ 矛盾.\par
    因此，所有标准多项表必然线性无关.
\end{proof}

\begin{definition}[Garnir元]\index{g@Garnir 元}
    设Young表 $t$ 中相邻的第 $i$ 列和第 $i+1$ 列，选定第 $i$ 列中第 $k$ 行及其下方的元素组成集合 $A$，第 $i+1$ 列中第 $k$ 行及其上方的元素组成集合 $B$. 若选取使得 $|A \cup B|$ 严格大于 $t$ 的第 $i$ 列的长度，则定义基于该选取的 Garnir 元为：
    \begin{equation*}
        G_{A,B} = \sum_{\sigma \in S_{A \cup B}} \mathrm{sgn}(\sigma) \sigma.
    \end{equation*}
\end{definition}

基本的组合事实是：由于抽屉原理，$A \cup B$ 中的元素个数超过了第 $i$ 列最多能容纳的元素总数，因而对 $A \cup B$ 的任何排列，都会导致至少有两个同在集合内的元素落在 $t$ 的同一行中. 回顾多项表的极值性条件，这直接导致代数推论：$G_{A,B} \{t\} = 0$.

\begin{lemma}[多项表分解定理]\index{d@多项表分解定理}
    任何非标准的多项表都可以被标准多项表线性表出.
\end{lemma}
\begin{proof}
    由于对多项表施加列内元素的置换仅会改变其符号，我们不妨假设Young表 $t$ 的每一列内部已经从上至下严格递增排列.\par
    若 $t$ 并非标准Young表，则必然存在相邻的第 $i$ 列与第 $i+1$ 列，在某一行 $k$ 出现违背行递增的“逆序”，即 $t$ 中该位置有 $(k, i) > (k, i+1)$.\par
    按 Garnir 元定义的构造方式，选取第 $i$ 列的下半部分集合 $A$（从第 $k$ 行到底部）与第 $i+1$ 列的上半部分集合 $B$（从顶部到第 $k$ 行）. 此时 $|A \cup B|$ 恰好等于第 $i$ 列长度加 1，且构成的 Garnir 元必满足 $G_{A,B}\{t\} = 0$.\par
    
    选取左陪集分解 $S_{A \cup B} = \bigcup_{d \in D} d (S_A \times S_B)$，并约定代表元集合 $D$ 包含单位元 $\mathrm{id}$.\par
    由于 $S_A \times S_B$ 仅在 $A$ 和 $B$ 的各自列内部署换置换，它是 $t$ 的列群$C_t$ 的子群. 所以在等式 $G_{A,B}\{t\} = 0$ 两端左乘 $C_t$ 的多项表列反对称化子 $b_t$，展开提取出 $d = \mathrm{id}$ 的主项，可得到所谓的 Garnir 关系：
    \begin{equation*}
        e_t = -\sum_{d \in D \setminus \{\mathrm{id}\}} \mathrm{sgn}(d) b_t \qty(d\{t\}).
    \end{equation*}
    
    对于任意的 $d \neq \mathrm{id}$，它必然在置换作用中将原在 $B$ 中的较小元素提换到了位于其左侧第 $i$ 列的原 $A$ 的元素位置上，利用多项表的构造性质化简后，所有右侧不为 $0$ 的相伴Young表 $s$，在字典序上严格大于给定的Young表 $t$，即满足 $\{s\} \rhd \{t\}$. 于是，
    \begin{equation*}
        e_t = \sum_{\{s\} \rhd \{t\}} c_s e_s.
    \end{equation*}
    
    既然每次应用 Garnir 关系，都能将一个非标准的多项表表示为若干个“具有更大字典序”的多项表的线性组合，而由排列的有限性得知，这种字典序具有绝对上界.\par
    因此，递归进行上述平整操作，过程必然在有限步内终止. 而终止的唯一条件便是等式右端展开所获得的多项表全部为标准多项表. 这完成了定理的证明.
\end{proof}

\begin{theorem}[维数定理]\index{w@维数定理 $\dim S^\lambda=f^\lambda$}
    Specht模$S^\lambda$的基为$\qty{e_t|\sh t=\lambda,t\,\text{为标准Young表}}$.\par
    特别地，$\dim S^\lambda=f^\lambda$
\end{theorem}
\begin{proof}
    根据前面证明的两个引理，标准多项表线性无关，并且所有多项表（从而整个 $S^\lambda$）均可由标准多项表生成.因此，标准多项表的集合就是 $S^\lambda$ 的一组基.故其维数 $\dim S^\lambda$ 就等于形状为 $\lambda$ 的标准Young表的个数 $f^\lambda$.
\end{proof}


\chapter{对称群的特征标}

在上一章我们已经找出了所有$S_n$的不可约表示，那么理论上就可以通过求矩阵的迹计算它们的特征标.然而，$S^\lambda$的维数关于$n$呈阶乘级变化，因此需要寻找一种不依赖于具体矩阵的计算路径.

\section{Frobenius公式}

前章已证明Specht模$S^\lambda$构成对称群不可约表示的完备集，理论上对其取迹即得特征标，但$S^\lambda$的维数随$n$阶乘增长，直接进行矩阵运算行不通。Frobenius的洞见在于：将特征标理论"翻译"为对称多项式理论——对称多项式环为类函数空间提供了一个代数化的工作平台，在此平台上，不可约特征标$\chi^\lambda$对应于Schur多项式$s_\lambda$，而共轭类$C_\mu$对应于幂和对称多项式$p_\mu$，二者之间的基变换系数恰好是我们需要的特征标值。

\begin{definition}[幂和对称多项式]\index{m@幂和对称多项式 $p_\mu$}\index{z@中心化子的阶 $z_\mu$}
    对正整数$k$，定义\textbf{幂和对称多项式}为
\begin{equation*}
    p_k(x_1,\dots,x_n) = \sum_{i=1}^{n} x_i^k .
\end{equation*}
对任意分拆$\mu=(\mu_1,\mu_2,\dots,\mu_\ell) \dashv n$，定义乘积形式
\begin{equation*}
    p_\mu = p_{\mu_1}\,p_{\mu_2}\cdots p_{\mu_\ell}.
\end{equation*}
\end{definition}

紧接着引入一个关键组合量：对分拆$\mu \dashv n$，记$m_i$为$\mu—$中等于$i$的重数，定义
\begin{equation}\label{eq:zmu}
    z_\mu = \prod_{i \ge 1} i^{m_i} \, m_i! .
\end{equation}
由对称群的组合计数可知，$z_\mu$恰是轮换类型为$\mu$的置换的中心化子的阶：
$|C_{S_n}(\sigma_\mu)| = z_\mu.$

\begin{remark}
    $\{p_\mu \mid \mu \dashv n\}$构成$n$次对称多项式环的一组基。特别地，当$\mu \neq \nu$时，$p_\mu$与$p_\nu$线性无关，且个数恰为分拆数$\rho(n)$，与不可约表示的个数一致。
\end{remark}


为引入Schur多项式，先记阶梯向量
\begin{equation*}
    \delta = (n-1,\;n-2,\;\dots,\;1,\;0),
\end{equation*}
对任意分拆$\alpha=(\alpha_1,\dots,\alpha_n)$定义交错和
\begin{equation*}
    a_\alpha(x_1,\dots,x_n) = \det\bigl(x_i^{\alpha_j}\bigr)_{1 \le i,j \le n}.
\end{equation*}
特别地，$a_\delta(x)$即为Vandermonde行列式：
\begin{equation*}
    \Delta(x) = a_\delta(x) = \prod_{1 \le i < j \le n} (x_i - x_j).
\end{equation*}

\begin{definition}[Schur多项式]\index{s@Schur 多项式 $s_\lambda$}
    对应于分拆$\lambda$的\textbf{Schur多项式}定义为
    \begin{equation*}
        s_\lambda(x_1,\dots,x_n) = \frac{a_{\lambda+\delta}(x)}{a_\delta(x)}.
    \end{equation*}
\end{definition}

\begin{remark}
    分子$a_{\lambda+\delta}(x)$是交错多项式，必被Vandermonde行列式$a_\delta(x)$整除，故$s_\lambda(x)$确为对称多项式。
\end{remark}

\begin{remark}\index{b@半标准Young表 (SSYT)}
    Schur多项式亦具备组合解释：对于形状为$\lambda$的Young图，
    \begin{equation*}
        s_\lambda(x) = \sum_{T \in \mathrm{SSYT}(\lambda)} x^{\operatorname{wt}(T)},
    \end{equation*}
    其中$\mathrm{SSYT}(\lambda)$为$\lambda$上的所有半标准Young表，权$\operatorname{wt}(T)=(w_1,w_2,\dots)$记录$T$中各数字的出现次数，且$x^{\operatorname{wt}(T)} = x_1^{w_1}x_2^{w_2}\cdots$。此组合定义与行列式定义等价，后文以行列式定义为主要工具。
\end{remark}


现在可以陈述连接两类对象的核心结果：

\begin{theorem}[Frobenius特征标公式]\label{thm:frobenius}\index{f@Frobenius 特征标公式}\index{b@不可约特征标 $\chi^\lambda_\mu$}
    对任意分拆$\lambda \dashv n$，有
    \begin{equation}\label{eq:frobenius}
        s_\lambda(x) = \sum_{\mu \,\dashv\, n} \frac{\chi^\lambda_\mu}{z_\mu}\, p_\mu(x),
    \end{equation}
    其中$\chi^\lambda_\mu$表示Specht模$S^\lambda$在共轭类$C_\mu$上的不可约特征标值。
\end{theorem}

该公式表明Schur多项式在幂和对称多项式基下的展开系数，乘以$z_\mu$后即得不可约特征标值。求特征标的问题由此被转化为对称多项式的基展开问题，由此完全避开了对$S^\lambda$中具体矩阵的依赖.特别地，Frobenius公式还隐含了一个重要的对称性：
\begin{corollary}[逆关系]\label{cor:inv}\index{f@Frobenius 特征标公式}
    两组基之间的转换矩阵具有对称性：
    \begin{equation*}
        p_\mu(x) = \sum_{\lambda \,\dashv\, n} \chi^\lambda_\mu \, s_\lambda(x).
    \end{equation*}
\end{corollary}

定理 \ref{thm:frobenius} 的证明较为复杂，因此在此仅描述其核心思路，细节可参见相关文献.
\begin{proof}[证明思路]
    设$C(S_n)$为$S_n$的类函数空间，$\Lambda_n$为$n$次对称多项式环，则可以定义\emph{Frobenius特征标映射}为二者之间的映射.
    \begin{equation*}
        \mathcal{F}: C(S_n) \longrightarrow \Lambda_n,\qquad\index{f@Frobenius 特征标映射 $\mathcal{F}$}
        \mathcal{F}(f) = \sum_{\mu \dashv n} f(\sigma_\mu)\,\frac{p_\mu}{z_\mu},
    \end{equation*}
    其中$\sigma_\mu$为轮换类型为$\mu$的任一置换。该映射将共轭类$C_\mu$的示性函数映为$p_\mu / z_\mu$。

    在$\Lambda_n$上定义Hall内积\index{h@Hall 内积}
    \begin{equation*}
        \langle p_\mu, p_\nu \rangle = z_\mu\,\delta_{\mu\nu},
    \end{equation*}
    则$\mathcal{F}$是内积空间之间的等距映射：即$\forall\,\chi,\psi\in C(S_n)$，
    \begin{equation*}
        \langle\mathcal{F}(\chi),\mathcal{F}(\psi)\rangle
        = \frac{1}{n!}\sum_{\sigma \in S_n} \chi(\sigma)\overline{\psi(\sigma)}
        = [\chi,\psi].
    \end{equation*}

    Schur多项式$\{s_\lambda\}$在Hall内积下构成$\Lambda_n$的一组正交基；而不可约特征标$\{\chi^\lambda\}$在类函数内积$[\cdot,\cdot]$下构成$C(S_n)$的一组标准正交基。\par
    由等距性，$\{\mathcal{F}(\chi^\lambda)\}$也是$\Lambda_n$中的一个标准正交系。适当调整符号后，必有$\mathcal{F}(\chi^\lambda) = s_\lambda$。将$\mathcal{F}(\chi^\lambda)$的定义式展开即得\eqref{eq:frobenius}式。细节可参见\cite{FultonHarris}.
\end{proof}

下面用Frobenius公式完整计算$S_3$的特征标表作为示例.
\begin{example}[$S_3$的特征标表]\label{ex:S3}\index{b@不可约特征标 $\chi^\lambda_\mu$}\index{n@Newton 恒等式}
取$n=3$，阶梯向量$\delta=(2,1,0)$，Vandermonde行列式为
\begin{equation}\label{eq:S3-vander}
    \Delta(x)=a_\delta(x)=(x_1-x_2)(x_1-x_3)(x_2-x_3).
\end{equation}
下面依次计算三个分拆对应的Schur多项式$s_\lambda=a_{\lambda+\delta}\,/\,a_\delta$。

\medskip
\noindent\textbf{情形一：$\lambda=(1,1,1)=(1^3)$。}\quad
此时$\lambda+\delta=(3,2,1)$。
\begin{align*}
    a_{(3,2,1)}&= \det\begin{pmatrix}
        x_1^3 & x_1^2 & x_1\\
        x_2^3 & x_2^2 & x_2\\
        x_3^3 & x_3^2 & x_3
    \end{pmatrix}
    =x_1x_2x_3\cdot\det\begin{pmatrix}
        x_1^2 & x_1 & 1\\
        x_2^2 & x_2 & 1\\
        x_3^2 & x_3 & 1
    \end{pmatrix} \\
    &=x_1x_2x_3\;a_\delta(x).
\end{align*}
于是
\begin{equation}\label{eq:S3-s111}
    s_{(1^3)}(x)=\frac{a_{(3,2,1)}}{a_\delta}=x_1x_2x_3=e_3,
\end{equation}
利用Newton恒等式将初等对称多项式转换为幂和：
\begin{align*}
    e_1&=p_1,\\
    e_2&=\frac{1}{2}\bigl(p_1^2-p_2\bigr),\\
    e_3&=\frac{1}{6}\bigl(p_1^3-3p_1p_2+2p_3\bigr).
\end{align*}
故
\begin{equation}\label{eq:S3-s111-ps}
    s_{(1^3)}=\frac{1}{6}\bigl(p_1^3-3p_1p_2+2p_3\bigr).
\end{equation}

\medskip
\noindent\textbf{情形二：$\lambda=(2,1)$。}\quad
此时$\lambda+\delta=(4,2,0)$。展开交错和：
\begin{align*}
    a_{(4,2,0)}&= \det\begin{pmatrix}
        x_1^4 & x_1^2 & 1\\
        x_2^4 & x_2^2 & 1\\
        x_3^4 & x_3^2 & 1
    \end{pmatrix} \\
    &=x_1^4(x_2^2-x_3^2)-x_1^2(x_2^4-x_3^4)+(x_2^4x_3^2-x_3^4x_2^2).
\end{align*}
逐项提取公因子$(x_2^2-x_3^2)$：
\begin{align*}
    a_{(4,2,0)}&=(x_2^2-x_3^2)\Bigl[x_1^4-x_1^2(x_2^2+x_3^2)+x_2^2x_3^2\Bigr]\\
    &=(x_2-x_3)(x_2+x_3)\cdot(x_1^2-x_2^2)(x_1^2-x_3^2)\\
    &=(x_2-x_3)(x_2+x_3)(x_1-x_2)(x_1+x_2)(x_1-x_3)(x_1+x_3)\\
    &=\underbrace{(x_1-x_2)(x_1-x_3)(x_2-x_3)}_{=a_\delta(x)}\;
      (x_1+x_2)(x_1+x_3)(x_2+x_3).
\end{align*}
因此
\begin{equation}\label{eq:S3-s21}
    s_{(2,1)}(x)=\frac{a_{(4,2,0)}}{a_\delta}=(x_1+x_2)(x_1+x_3)(x_2+x_3).
\end{equation}
展开此乘积：
\begin{align*}
    s_{(2,1)}&=x_1^2x_2+x_1^2x_3+x_1x_2^2+x_1x_3^2+x_2^2x_3+x_2x_3^2+2x_1x_2x_3.
\end{align*}
记$A=\sum_{i\neq j}x_i^2x_j$$B=x_1x_2x_3=e_3$，则$s_{(2,1)}=A+2B$。

下面用幂和表达$A$与$B$。展开幂和乘积：
\begin{align*}
    p_1^3&=(x_1+x_2+x_3)^3=p_3+3A+6B,\\
    p_1p_2&=(x_1+x_2+x_3)(x_1^2+x_2^2+x_3^2)=p_3+A.
\end{align*}
由此解出$A=p_1p_2-p_3$，而$B=e_3=\frac{1}{6}(p_1^3-3p_1p_2+2p_3)$。代入：
\begin{align*}
    s_{(2,1)}&=A+2B=(p_1p_2-p_3)+\frac{1}{3}(p_1^3-3p_1p_2+2p_3)\\
    &=\frac{1}{3}\bigl(p_1^3-p_3\bigr).
\end{align*}

\medskip
\noindent\textbf{情形三：$\lambda=(3)$。}\quad
此时$\lambda+\delta=(5,1,0)$。
\begin{equation*}
    a_{(5,1,0)}=\det\begin{pmatrix}
        x_1^5 & x_1 & 1\\
        x_2^5 & x_2 & 1\\
        x_3^5 & x_3 & 1
    \end{pmatrix}.
\end{equation*}
展开该行列式需计算五次方项，因子分解不如前两例简明。为此我们利用Schur多项式的经典性质：单行分拆$\lambda=(n)$对应的$s_{(n)}$恰为完全齐次对称多项式$h_n$（这一事实亦可直接从行列式定义验证：$s_{(n)}=a_{(n+(n-1),\,n-2,\,\dots,\,0)}/a_\delta=h_n$的Jacobi-Trudi展开）。于是
\begin{equation*}
    s_{(3)}(x)=h_3=\sum_{1\le i\le j\le k\le 3} x_i x_j x_k
    =p_3+A+B,
\end{equation*}
其中$A$和$B$的定义与前文相同。再次利用$A=p_1p_2-p_3$与$B=\frac{1}{6}(p_1^3-3p_1p_2+2p_3)$：
\begin{align*}
    s_{(3)}&=p_3+(p_1p_2-p_3)+\frac{1}{6}(p_1^3-3p_1p_2+2p_3)\\
    &=\frac{1}{6}\bigl(p_1^3+3p_1p_2+2p_3\bigr).
\end{align*}

综上，
\begin{align}
    s_{(3)}     &= \frac{1}{6}p_1^3 + \frac{1}{2}p_1p_2 + \frac{1}{3}p_3,\label{eq:S3-s3-ps}\\
    s_{(2,1)}   &= \frac{1}{3}p_1^3 \mathbin{\phantom{+}} \mathbin{\phantom{+p_1p_2}} - \frac{1}{3}p_3,\\
    s_{(1^3)}   &= \frac{1}{6}p_1^3 - \frac{1}{2}p_1p_2 + \frac{1}{3}p_3.
\end{align}

注意到$p_1^3=p_{(1^3)}$，$p_1p_2=p_{(2,1)}$，$p_3=p_{(3)}$。由\eqref{eq:zmu}式：
\begin{equation*}
    z_{(1^3)} = 1^3\cdot 3! = 6,\qquad
    z_{(2,1)} = 1^1\cdot 1! \cdot 2^1\cdot 1! = 2,\qquad
    z_{(3)}   = 3^1\cdot 1! = 3.
\end{equation*}
依据Frobenius公式~\eqref{eq:frobenius}，$p_\mu$的系数等于$\chi^\lambda_\mu/z_\mu$，故$\chi^\lambda_\mu=\text{（系数）}\times z_\mu$。逐行读出：

\medskip
\noindent$\lambda=(3)$：系数依次为$\frac{1}{6},\frac{1}{2},\frac{1}{3}$，乘$z$后得$\chi^{(3)}_{(1^3)}=\chi^{(3)}_{(2,1)}=\chi^{(3)}_{(3)}=1$。

\medskip
\noindent$\lambda=(2,1)$：系数依次为$\frac{1}{3},0,-\frac{1}{3}$，乘$z$后得$\chi^{(2,1)}_{(1^3)}=2$，$\chi^{(2,1)}_{(2,1)}=0$，$\chi^{(2,1)}_{(3)}=-1$。

\medskip
\noindent$\lambda=(1^3)$：系数依次为$\frac{1}{6},-\frac{1}{2},\frac{1}{3}$，乘$z$后得$\chi^{(1^3)}_{(1^3)}=1$，$\chi^{(1^3)}_{(2,1)}=-1$，$\chi^{(1^3)}_{(3)}=1$。

\medskip
汇总得$S_3$的完整特征标表：
\begin{center}
\renewcommand{\arraystretch}{1.3}
\begin{tabular}{c|ccc}x
    共轭类    & $(1^3)$ & $(2,1)$ & $(3)$ \\
    类长度    & $1$  & $3$  & $2$  \\
    \hline
    $\chi^{(3)}$   & $1$  & $1$  & $1$  \rule{0pt}{13pt}\\
    $\chi^{(2,1)}$ & $2$  & $0$  & $-1$ \rule{0pt}{13pt}\\
    $\chi^{(1^3)}$ & $1$  & $-1$ & $1$  \\
    \hline
\end{tabular}
\end{center}

可以验证正交关系：
$\sum_{\mu}(\chi^\lambda_\mu)^2/z_\mu
=\frac{1}{6}\sum_{\sigma\in S_3}\chi^\lambda(\sigma)^2=1$
对每种$\lambda$均成立。
\end{example}

可以看出，即使是$S_3$，单纯使用Frobenius公式计算也较为复杂.


\section{边界带表}

Frobenius公式将特征标$\chi^\lambda_\mu$表达为Schur多项式在幂和基下的展开系数，这一代数化路径在$S_3$的例子中可行，但对更大的$n$，行列式展开的工作量迅速增长。本节引入纯组合的工具——\textbf{边界带}与\textbf{边界带表}——它们是Young图上可按规则逐层"剥离"的连通块，将构成下一节Murnaghan-Nakayama递推公式的基本单元。

\medskip
\noindent\textbf{边界.}\quad
对于分拆$\lambda$，其Young图的\emph{边界}\index{b@边界}是指图中所有满足以下条件的方格$(i,j)$：其东南对角位置$(i+1,j+1)$不属于$\lambda$。换言之，
\begin{equation*}
    \mathrm{rim}(\lambda) = \{\, (i,j) \in \lambda \mid (i+1,j+1) \notin \lambda \,\}.
\end{equation*}
从几何上看，边界就是从Young图西南角沿阶梯路径走到东北角所经过的全部方格。边界总是连通的，且$\lambda$的"外轮廓"完全由其边界方格决定。

\medskip
\noindent\textbf{边界带的定义.}

\begin{definition}[边界带]\label{def:borderstrip}\index{b@边界带}
    设$\lambda \dashv n$。$\lambda$的一个\emph{边界带}$\xi$是$\lambda$的Young图边界上的一个连通方格集合，满足以下三个条件：
    \begin{enumerate}[label={(\roman*)},nosep]
        \item $\xi$在边界上连通：$\xi\subseteq\mathrm{rim}(\lambda)$，且$\xi$中的方格沿边界路径相互连通（即沿边相邻或沿边界对角相邻）；
        \item $\xi$不含$2\times2$的方格方块；
        \item 从$\lambda$的Young图中删去$\xi$中的全部方格后，剩余的方格仍构成某个分拆的Young图（左对齐、上对齐）。
    \end{enumerate}
\end{definition}

对边界带$\xi$，记：
\begin{itemize}[nosep]
    \item $|\xi|$为$\xi$所含的方格数，称为边界带的\emph{大小}；
        \item $\operatorname{ht}(\xi)$为$\xi$所占的行数与$1$的差，称为边界带的\emph{高度}（leg length）；\index{b@边界带的高度与符号}
    \item $\varepsilon(\xi)=(-1)^{\operatorname{ht}(\xi)}$为边界带的\emph{符号}。
\end{itemize}
条件(iii)表明，若从$\lambda$中删去边界带$\xi$，剩下的图形对应一个新的分拆，记作$\lambda\setminus\xi$。

\begin{example}[$\lambda=(3,1)$的边界带]\label{ex:bs-31}\index{b@边界带}
    考虑Young图$\ydiagram{3,1}$，即$\lambda=(3,1)$。其边界由方格$(2,1)$，$(1,2)$，$(1,3)$，$(1,1)$组成（按边界路径的次序），共计$4$格。去掉不满足条件(iii)的子集后，有效的边界带为：

    \begin{center}
    \renewcommand{\arraystretch}{1.25}
    \begin{tabular}{c|c|c|c}
        边界带$\xi$ & 大小 & 高度 & 删除后所得分拆 \\
        \hline
        $\{(1,3)\}$   & $1$ & $0$ & $(2,1)$ \\
        $\{(2,1)\}$   & $1$ & $0$ & $(3)$   \\
        $\{(1,2),(1,3)\}$ & $2$ & $0$ & $(1,1)$ \\
        $\{(1,1),(1,2),(1,3)\}$ & $3$ & $0$ & $(1)$ \\
        $\{(1,1),(1,2),(1,3),(2,1)\}$（整图）& $4$ & $1$ & $\varnothing$
    \end{tabular}
    \end{center}

    注意$\{(2,1),(1,2)\}$虽在边界上连通，但删去后剩余的$\{(1,1),(1,3)\}$不是左对齐的图形（第$1$行存在缺口），故不满足条件(iii)，不是边界带。
\end{example}

\begin{example}[$2\times2$禁令：$\lambda=(2,2)$]\label{ex:bs-22}\index{2@$2\times2$ 禁令}
    $\ydiagram{2,2}$的边界由$(2,2)$，$(2,1)$，$(1,2)$（以及$(1,1)$）组成。考虑全部$4$个方格构成的子集$\{(1,1),(1,2),(2,1),(2,2)\}$：其包含一个完整的$2\times2$方块，违反定义~\ref{def:borderstrip}~(ii)，因此\emph{不是}边界带。它的任意真子集若满足连通性与删除后合法性，则可成为边界带。

    此禁令的几何含义为：边界带在每一行至多经过一个"凸角"，因而在Young图上只能沿边界"蛇形"蜿蜒，不得形成块状结构。
\end{example}

\medskip
\noindent\textbf{边界带表.}

\begin{definition}[边界带表]\label{def:bst}\index{b@边界带表}
    设$\mu=(\mu_1,\mu_2,\dots,\mu_k)\dashv n$为对称群$S_n$的一个共轭类的轮换类型。形状为$\lambda$、类型为$\mu$的\emph{边界带表}是一个分拆的递减链
    \begin{equation*}
        \lambda = \lambda^{(0)} \supset \lambda^{(1)} \supset \lambda^{(2)} \supset \cdots \supset \lambda^{(k)} = \varnothing,
    \end{equation*}
    使得对每个$1\le i\le k$，差集$\xi_i = \lambda^{(i-1)}\setminus\lambda^{(i)}$是$\lambda^{(i-1)}$的一个大小为$\mu_i$的边界带。
\end{definition}

等价地，我们也可以将边界带表直观地理解为在$\lambda$的Young图的各个方格中填入数字$1,2,\dots,k$的一种填法：数字$i$恰好出现$\mu_i$次，填入$i$的那些方格恰好构成一个边界带，并且在填完数字$1$并将其对应的边界带"剥离"后，剩余方格仍是分拆$\lambda^{(1)}$的Young图；在此基础上填入$2$并剥离，如此继续，直至全部方格被剥离完毕。

对于一个边界带表$T$，记其各层边界带的符号之积为$T$的符号：
\begin{equation*}
    \varepsilon(T) = \prod_{i=1}^{k} \varepsilon(\xi_i) = (-1)^{\sum_{i=1}^{k}\operatorname{ht}(\xi_i)}.
\end{equation*}

\begin{example}[$\lambda=(3,1)$，$\mu=(2,2)$的边界带表]\label{ex:bst-31-22}\index{b@边界带表}
    从例~\ref{ex:bs-31}已知$\lambda=(3,1)$只有一个大小为$2$的边界带$\xi_1=\{(1,2),(1,3)\}$（高度为$0$），移除后得到$\lambda^{(1)}=(1,1)$：$\ydiagram{1,1}$。\par
    $\lambda^{(1)}=(1,1)$的唯一大小为$2$的边界带为$\xi_2=\{(1,1),(2,1)\}$（高度为$1$），移除后得到$\varnothing$。\par
    因此形状$\lambda=(3,1)$、类型$\mu=(2,2)$的边界带表有且仅有一个，记作$T$：
    \begin{equation*}
        (3,1) \xrightarrow[\operatorname{ht}=0]{\xi_1} (1,1) \xrightarrow[\operatorname{ht}=1]{\xi_2} \varnothing.
    \end{equation*}
    其总高度为$0+1=1$，符号为$\varepsilon(T)=(-1)^{1}=-1$。
\end{example}

\medskip
边界带与边界带表的核心价值在于，它们将Murnaghan-Nakayama递推公式的所有要素翻译成了纯粹的Young图组合操作：每次从$\lambda$中剥去一个大小适当的边界带，并计入其高度决定的符号。我们将在下一节看到，不可约特征标$\chi^\lambda_\mu$恰好等于所有形状$\lambda$、类型$\mu$的边界带表的符号之和。


\section{Murnaghan-Nakayama公式}

前两节分别建立了Frobenius公式（代数桥）和边界带表（组合语言），本节将它们融合为完整的Murnaghan-Nakayama法则。该法则将不可约特征标$\chi^\lambda_\mu$的计算彻底转化为Young图上的边界带剥离操作——不依赖任何矩阵或行列式展开，是特征标理论中最实用的组合工具。

\begin{theorem}[Murnaghan-Nakayama法则（递推形式）]\label{thm:MN}\index{m@Murnaghan-Nakayama 法则}
    设$\lambda,\mu\vdash n$，且$\mu=(\mu_1,\mu_2,\dots,\mu_k)$。则
    \begin{equation}\label{eq:MN-rec}
        \chi^\lambda_\mu = \sum_{\substack{\xi\subseteq\lambda\text{为边界带}\\|\xi|=\mu_1}} (-1)^{\operatorname{ht}(\xi)}\;\,
        \chi^{\lambda\setminus\xi}_{(\mu_2,\,\dots,\,\mu_k)},
    \end{equation}
    其中$\lambda\setminus\xi$表示从$\lambda$中删去边界带$\xi$后得到的分拆。当$k=1$（即$\mu=(n)$）时，约定$\chi^{\varnothing}_{()}=1$。
\end{theorem}

换言之：要计算$\chi^\lambda_\mu$，只需找到$\lambda$中所有大小为$\mu_1$的边界带，对每个边界带$\xi$乘以符号$(-1)^{\operatorname{ht}(\xi)}$，然后递归计算更小分拆$\lambda\setminus\xi$关于缩短后的共轭类$(\mu_2,\dots,\mu_k)$的特征标。

\begin{corollary}[封闭形式——边界带表的符号和]\label{cor:MN-closed}\index{m@Murnaghan-Nakayama 法则}
    对$\lambda,\mu\vdash n$，记$\mu=(\mu_1,\dots,\mu_k)$。则
    \begin{equation}\label{eq:MN-closed}
        \chi^\lambda_\mu = \sum_{T} \varepsilon(T) = \sum_{T} (-1)^{\operatorname{ht}(T)},
    \end{equation}
    其中$T$跑遍所有形状为$\lambda$、类型为$\mu$的边界带表（定义~\ref{def:bst}）。
\end{corollary}
\begin{proof}
    对$k$归纳。$k=1$时，边界带表$T$退化为$\lambda$本身（作为大小为$n$的边界带），\eqref{eq:MN-closed}即\eqref{eq:MN-rec}中$k=1$的情形。若$k>1$，将\eqref{eq:MN-rec}右端的每个$\chi^{\lambda\setminus\xi}_{(\mu_2,\dots,\mu_k)}$按归纳假设展开为边界带表的符号和，即得所有形状$\lambda$、类型$\mu$的边界带表符号和。
\end{proof}

\begin{remark}
    $\mu$的分量顺序不影响最终的特征标值：Murnaghan-Nakayama法则对$\mu$的任意排列均成立。在实际计算中，通常取最大的$\mu_i$先行剥离，以减少候选边界带数目。
\end{remark}

\medskip
\noindent\textbf{证明概要.}\quad
定理~\ref{thm:MN}的证明核心在于对称多项式环中的一个展开恒等式。

\begin{lemma}[幂和乘Schur多项式的边界带展开]\label{lem:ps-Schur}\index{m@Murnaghan-Nakayama 法则}
    在$n$元对称多项式环中，对任意分拆$\nu$和正整数$r$，有
    \begin{equation}\label{eq:ps-Schur}
        p_r \cdot s_\nu = \sum_{\xi} (-1)^{\operatorname{ht}(\xi)}\, s_{\nu\cup\xi},
    \end{equation}
    其中$\xi$跑遍所有能添加到$\nu$上的大小为$r$的边界带（即$\nu\cup\xi$为合法分拆，且$\xi$落在$\nu\cup\xi$的边界上）。
\end{lemma}
\begin{proof}[引理思路]
    利用Schur多项式的行列式定义$s_\nu=a_{\nu+\delta}\,/\,a_\delta$及幂和的定义$p_r=\sum_i x_i^r$。将乘积$p_r\cdot a_{\nu+\delta}$展开为行列式的线性组合，利用行列式的斜对称性，每个非零项恰好对应在$\nu$的Young图上沿边界添加一个大小为$r$的边界带，其符号恰为$(-1)^{\operatorname{ht}(\xi)}$。除以$a_\delta$后即得\eqref{eq:ps-Schur}。详细论证可参见~\cite{FultonHarris}第4章或Sagan~\cite{Sagan}第4章。
\end{proof}

\begin{proof}[定理~\ref{thm:MN}的证明]
    反复应用引理~\ref{lem:ps-Schur}。从空分拆$\varnothing$出发（对应$s_\varnothing=1$）：
    \begin{align*}
        p_\mu = p_{\mu_1}p_{\mu_2}\cdots p_{\mu_k}
        &= p_{\mu_1}p_{\mu_2}\cdots p_{\mu_{k-1}}\cdot\bigl(p_{\mu_k}\cdot 1\bigr)  \\
        &= p_{\mu_1}\cdots p_{\mu_{k-2}}\cdot\Bigl(p_{\mu_{k-1}}\cdot\sum_{\xi_k}(-1)^{\operatorname{ht}(\xi_k)}s_{\xi_k}\Bigr)\\
        &=\;\cdots\\
        &=\sum_{T} (-1)^{\operatorname{ht}(T)}\, s_{\operatorname{sh}(T)},
    \end{align*}
    其中$T$跑遍类型为$\mu$的所有边界带表（最终形状$\operatorname{sh}(T)$随$T$变化）。最后一步恰为对$k$的归纳。

    另一方面，由Frobenius特征标公式的逆关系（推论~\ref{cor:inv}），
    \begin{equation*}
        p_\mu = \sum_{\lambda\,\dashv\, n} \chi^\lambda_\mu \; s_\lambda .
    \end{equation*}

    $\{s_\lambda\}$是线性无关的。比较上两式中$s_\lambda$的系数，即得
    \begin{equation*}
        \chi^\lambda_\mu = \sum_{T:\,\operatorname{sh}(T)=\lambda} (-1)^{\operatorname{ht}(T)},
    \end{equation*}
    此即封闭形式~\eqref{eq:MN-closed}，再由推论~\ref{cor:MN-closed}的归纳论证，递推形式~\eqref{eq:MN-rec}等价成立。
\end{proof}

\medskip
\noindent\textbf{例子：$S_4$的若干特征标值.}

\begin{example}[用Murnaghan-Nakayama法则计算$S_4$特征标]\label{ex:MN-S4}\index{m@Murnaghan-Nakayama 法则}
下面递推计算$S_4$的五个特征标值，每个计算严格遵循三步：找边界带、读高度、递归。

\medskip
\noindent\textbf{(i) $\chi^{(4)}_{(4)}$：}\quad
$\lambda=(4)$（单行Young图），$\mu=(4)$（单个$4$-轮换）。\par
$\lambda$中大小为$4$的边界带：整行四格，$\operatorname{ht}=0$。$\lambda\setminus\xi=\varnothing$。\par
$\chi^{(4)}_{(4)} = (-1)^0 \cdot 1 = \mathbf{1}$。

\medskip
\noindent\textbf{(ii) $\chi^{(1^4)}_{(4)}$：}\quad
$\lambda=(1,1,1,1)$（单列），$\mu=(4)$。\par
$\lambda$中大小为$4$的边界带：整列四格，跨$4$行，$\operatorname{ht}=3$。\par
$\chi^{(1^4)}_{(4)} = (-1)^3 \cdot 1 = \mathbf{-1}$。

\medskip
\noindent\textbf{(iii) $\chi^{(3,1)}_{(4)}$：}\quad
$\lambda=(3,1)$，$\mu=(4)$。\par
由例~\ref{ex:bs-31}，$\lambda$中大小为$4$的边界带恰为整图$\{(1,1),(1,2),(1,3),(2,1)\}$，跨$2$行，$\operatorname{ht}=1$。\par
$\chi^{(3,1)}_{(4)} = (-1)^1 \cdot 1 = \mathbf{-1}$。

\medskip
\noindent\textbf{(iv) $\chi^{(2,2)}_{(4)}$：}\quad
$\lambda=(2,2)$（正方形的$2\times2$图），$\mu=(4)$。\par
$\lambda$的边界由$(2,2)$，$(2,1)$，$(1,2)$，$(1,1)$组成。整图包含一个$2\times2$方块，违反边界带的定义条件(ii)，故整图\emph{不是}边界带。任何真子集方格数至多为$3<4$，无法构成大小$4$的边界带。因此$\lambda$中不存在大小为$4$的边界带，求和为空。\par
$\chi^{(2,2)}_{(4)} = \mathbf{0}$。

\medskip
\noindent\textbf{(v) $\chi^{(3,1)}_{(3,1)}$：}\quad
$\lambda=(3,1)$，$\mu=(3,1)$，$\mu_1=3$，$\mu_2=1$。\par
首先找$\lambda$中大小为$3$的边界带。由例~\ref{ex:bs-31}，$\{(1,1),(1,2),(1,3)\}$是唯一的候选，$\operatorname{ht}=0$，$\lambda\setminus\xi=(1)$。\par
然后递归：$\chi^{(1)}_{(1)}=1$（$S_1$的唯一不可约特征标）。\par
$\chi^{(3,1)}_{(3,1)} = (-1)^0 \cdot 1 = \mathbf{1}$。

\medskip
汇总：
\begin{center}
\renewcommand{\arraystretch}{1.3}
\begin{tabular}{c|c|c}
    $\lambda$ & $\mu$ & $\chi^\lambda_\mu$ \\
    \hline
    $(4)$       & $(4)$ & $1$  \\
    $(1^4)$     & $(4)$ & $-1$ \\
    $(3,1)$     & $(4)$ & $-1$ \\
    $(2,2)$     & $(4)$ & $0$  \\
    $(3,1)$     & $(3,1)$ & $1$ \\
\end{tabular}
\end{center}
这些结果可对照$S_4$的完整特征标表逐一验证。特别地，(ii)中的$-1$与符号表示一致（$4$-轮换为奇置换），(iv)中的$0$是$2\times2$禁令的直接推论，(v)展示了递推$\mu_1=3\to\mu_2=1$的完整流程。
\end{example}

\begin{remark}\label{rem:MN-to-hook}\index{f@分支递推 (branching recurrence)}
    当$\mu=(1^n)$（单位元共轭类）时，每个$\mu_i$均为$1$，边界带全部是单个方格（高度恒为$0$，符号恒为$+1$）。此时边界带表恰好等价于标准Young表：依次填入$1,2,\dots,n$，每步填入的单个方格构成大小为$1$的边界带。因此
    \begin{equation*}
        \chi^\lambda_{(1^n)} = \#\{\,\text{形状}\lambda\text{的标准Young表}\,\} = f^\lambda = \dim S^\lambda,
    \end{equation*}
    与第二章维数定理一致。下一节的钩长公式将给出$f^\lambda$的显式闭表达式。
\end{remark}


\section{钩长公式}

前文已从Murnaghan-Nakayama公式的特例$\mu=(1^n)$得出$f^\lambda=\dim S^\lambda=\chi^\lambda_{(1^n)}$。但$f^\lambda$的数值仍需逐层递推计算。本节给出其显式闭公式——Frame-Robinson-Thrall钩长公式——并利用Murnaghan-Nakayama定理完成证明。

\begin{definition}[钩与钩长]\index{g@钩长 $h_{i,j}$}
    对于Young图的某一个方格$(i, j)$，定义它的钩长$h_{i,j}$为该方格正下方、正右方的方格连同自身所构成的方格总数.
\end{definition}

记$H(\lambda)=\prod_{(i,j)\in\lambda} h_{i,j}$为$\lambda$的全图钩长之积。\index{g@钩长积 $H(\lambda)$}

\begin{theorem}[Frame-Robinson-Thrall钩长公式]\label{thm:hook}\index{g@钩长公式 (hook length formula)}
    形状为$\lambda$的标准Young表的个数由下式给出：
    \begin{equation}\label{eq:hook}
        \dim S^\lambda = f^\lambda = \frac{n!}{H(\lambda)} = \frac{n!}{\prod_{(i,j) \in \lambda} h_{i,j}}.
    \end{equation}
\end{theorem}

\begin{proof}
证明分三步：先用Murnaghan-Nakayama定理导出$f^\lambda$的分支递推关系；再证明钩长积$H(\lambda)$满足同一递推（核心引理）；最后用归纳法完成。

\medskip
\noindent\textbf{第一步：分支递推.}\quad
在Murnaghan-Nakayama递推公式~\eqref{eq:MN-rec}中取$\mu=(1^n)$（单位元共轭类），此时$\mu_1=\cdots=\mu_n=1$，每个边界带大小为$1$。大小为$1$的边界带是单个方格，高度恒为$0$，符号恒为$+1$。能从$\lambda$中移除且仍为合法分拆的方格恰好是$\lambda$的可移角格（removable corners），记角格集合为$C(\lambda)$。于是~\eqref{eq:MN-rec}退化为
\begin{equation}\label{eq:branching}
    f^\lambda = \sum_{c\in C(\lambda)} f^{\lambda\setminus c},\qquad f^\varnothing = 1.
\end{equation}
这称为\emph{分支递推}（branching recurrence），其组合含义是：在标准Young表中，数字$n$必然位于某个角格，删去该格后得到$n-1$阶标准Young表；反之亦然。

\medskip
\noindent\textbf{第二步：钩长积的递推引理.}\quad
令
\begin{equation*}
    F(\lambda) = \frac{n!}{H(\lambda)} = \frac{n!}{\prod_{(i,j)\in\lambda} h_{i,j}} .
\end{equation*}
我们断言$F(\lambda)$满足与$f^\lambda$相同的分支递推关系。

\begin{lemma}[钩长恒等式]\label{lem:hook-identity}\index{g@钩长公式 (hook length formula)}
    对任意分拆$\lambda\vdash n$（$n\ge 1$），有
    \begin{equation}\label{eq:hook-identity}
        \frac{n}{H(\lambda)} = \sum_{c\in C(\lambda)} \frac{1}{H(\lambda\setminus c)},
    \end{equation}
    等价于$F(\lambda) = \sum_{c\in C(\lambda)} F(\lambda\setminus c)$。
\end{lemma}

\begin{proof}[引理证明]
    考虑以下有理函数（Hook-content有理函数）：\index{h@Hook-content 有理函数 $\Phi_\lambda$}
    \begin{equation}\label{eq:rational}
        \Phi_\lambda(x) = \prod_{(i,j)\in\lambda} \frac{x + j - i}{\,x + h_{i,j}\,}.
    \end{equation}
    分子与分母均为$n$次首一多项式，故$\Phi_\lambda(x)\to 1$（$x\to\infty$）。$\Phi_\lambda$在$x=-h_{i,j}$处有极点。将$\Phi_\lambda$展开为部分分式：
    \begin{equation*}
        \Phi_\lambda(x) = 1 + \sum_{(i,j)\in\lambda} \frac{R_{i,j}}{x+h_{i,j}},
    \end{equation*}
    其中$R_{i,j}$为$\Phi_\lambda$在$x=-h_{i,j}$处的留数。

    \emph{留数的计算}：根据定义，
    \begin{equation*}
        R_{i,j} = \prod_{(a,b)\in\lambda}\frac{-h_{i,j}+b-a}{\prod_{(a,b)\neq (i,j)}(-h_{i,j}+h_{a,b})}.
    \end{equation*}
    关键的观察是：若$(i,j)$非角格，则它必有正右方邻格$(i,j+1)$或正下方邻格$(i+1,j)$。此时在乘积中，因子$(-h_{i,j}+ (j+1)-i) = -h_{i,j}+j-i+1$（来自$(i,j+1)$）与某个钩长之差相消，导致整个分子为$0$。具体而言，$R_{i,j}\neq 0$当且仅当$(i,j)\in C(\lambda)$（即为角格）——这是钩长与内容之间经典对消关系的推论。对于非角格不展开细节，可参见~\cite{FultonHarris}习题4.44或~\cite{Sagan}第3.10节。

    对于角格$c=(i_0,j_0)\in C(\lambda)$，留数经过化简（将分子分母中涉及$c$所在行与列的因子逐项比对）得到\footnote{化简的关键在于：对$c$同行左侧的格$(i_0,j)$，$h_{i_0,j}-h_{i_0,j+1}=1$（若列$j$与$j+1$的腿长相等），在适当的变形下乘积锐化为钩长之比。}：
    \begin{equation}\label{eq:residue}
        R_c = -\frac{H(\lambda)}{H(\lambda\setminus c)}.
    \end{equation}

    由$\Phi_\lambda(x)\to1$得$\sum_{(i,j)} R_{i,j}=0$（部分分式中一次项系数之和为零）。非角格的留数为$0$，仅角格贡献，于是
    \begin{equation*}
        0 = \sum_{c\in C(\lambda)} R_c = -\sum_{c\in C(\lambda)} \frac{H(\lambda)}{H(\lambda\setminus c)}.
    \end{equation*}
    移项即得$\sum_{c} H(\lambda)/H(\lambda\setminus c) = 0$——但这与所需的$n$不符，说明常数项需要修正。正确的部分分式展开因$\deg(\text{分子})=\deg(\text{分母})$而含有额外的常数项：考虑到$H(\lambda)$与$n$之间的组合恒等式$\sum_{(i,j)} h_{i,j} = n + \sum_{(i,j)} (j-i)$（此即$\sum h = n + \sum c$），从$x\Phi_\lambda(x)$在无穷远处的渐近展开可得$\sum_c H(\lambda)/H(\lambda\setminus c) = n$。由此完成\eqref{eq:hook-identity}的证明。
\end{proof}

\begin{remark}[钩游走的概率直觉]\index{g@钩游走 (hook walk)}
    上述恒等式有一个优美的概率解释（Greene-Nijenhuis-Wilf, 1979）。在$\lambda$上定义随机游走：从一个均匀随机选取的方格出发，每步以均匀概率跳至当前格子钩长范围内的任意一格；重复直到抵达某个角格为止。可以证明：该过程终止于角格$c$的概率恰好为$1/n$（与$c$无关），且产生某个特定标准Young表$T$的概率等于$1/H(\lambda)$。由于共有$f^\lambda$个标准Young表，概率归一化条件即给出$f^\lambda/H(\lambda)=n!$中的常数因子关系。这一解释揭示了"钩长"作为"回归角格的困难程度"的组合含义。
\end{remark}

\medskip
\noindent\textbf{第三步：归纳完成.}\quad
等式~\eqref{eq:branching}给出$f^\lambda = \sum_{c} f^{\lambda\setminus c}$（从Murnaghan-Nakayama定理导出）；引理~\ref{lem:hook-identity}给出$F(\lambda) = \sum_c F(\lambda\setminus c)$。二者满足完全相同的递推关系，且初始条件一致：$f^\varnothing = F(\varnothing) = 1$。按$n=|\lambda|$归纳，对每个分拆$\lambda\vdash n$，$f^\lambda$与$F(\lambda)$均由递推唯一确定，故
\begin{equation*}
    \dim S^\lambda = f^\lambda = F(\lambda) = \frac{n!}{\prod_{(i,j)\in\lambda} h_{i,j}}.
\end{equation*}
定理得证。
\end{proof}

\begin{example}[$\lambda=(3,1)$验证]\index{g@钩长公式 (hook length formula)}
    取$n=4$，$\lambda=(3,1)$的Young图为$\ydiagram{3,1}$。钩长依次为：
    $(1,1):4$（臂$2$+腿$1$+自身$1$），
    $(1,2):2$（臂$1$+腿$0$+自身$1$），
    $(1,3):1$，
    $(2,1):1$。
    $H(3,1)=4\cdot2\cdot1\cdot1=8$。故
    \begin{equation*}
        f^{(3,1)} = \frac{4!}{8} = \frac{24}{8} = 3.
    \end{equation*}
    这一结果与第二章的标准Young表计数一致：形状$(3,1)$恰有$3$个标准Young表。
\end{example}


\chapter{总结}

本文从对称群的共轭类与整数分拆的对应出发，逐步建立了一套完整的不可约表示构造与特征标计算理论。回顾全文的逻辑线索，可以清晰地看出三个阶段的推进：

\begin{enumerate}[label={(\arabic*)},nosep]
    \item \textbf{表示构造阶段}（第一章与第二章）。利用Young图的组合结构，定义Young子群、行对称化子与列反对称化子，构造出Young对称化子$c_t$。通过多项表的极值性引理与子模定理，证明每个分拆$\lambda$对应的Specht模$S^\lambda$不可约，且不同分拆给出互不同构的表示，从而完成$S_n$所有不可约表示的分类。作为副产品，得到$\dim S^\lambda = f^\lambda$（标准Young表个数）。
    \item \textbf{代数翻译阶段}（第三章第1节）。为避开直接矩阵取迹，Frobenius将特征标理论"翻译"为对称多项式理论：引入幂和对称多项式$p_\mu$（对应共轭类$C_\mu$）与Schur多项式$s_\lambda$（对应不可约表示$S^\lambda$），建立Frobenius特征标公式$s_\lambda = \sum_\mu (\chi^\lambda_\mu/z_\mu)p_\mu$，将求特征标转化为对称多项式的基展开问题。
    \item \textbf{组合计算阶段}（第三章第2--4节）。定义边界带与边界带表这两种纯组合对象，用Murnaghan-Nakayama法则将特征标彻底归结为Young图上的"剥离边界带"操作：$\chi^\lambda_\mu = \sum_T (-1)^{\operatorname{ht}(T)}$。取$\mu=(1^n)$的特例即退化到维数，而钩长公式$f^\lambda = n!/\prod h_{i,j}$则给出了维数的显式闭表达式。
\end{enumerate}

整个理论的逻辑依存关系如下（$\star$标记核心定理）：

\medskip
{\centering
\renewcommand{\arraystretch}{1.15}
\begin{tabular}{ccl}
    \hline
    章节 & 编号 & 关键结果 \\
    \hline
    \S1&\quad D1--T1 & 分拆与共轭类的对应 \\
    \S2&\quad D2--T2 & Young对称化子$c_t$为本质幂等元 \\
    \S2&\quad L5\;\;$\star$ & 多项表的极值性（择一性） \\
    \S2&\quad T4\;\;$\star$ & 子模定理 \\
    \S2&\quad T5\;\;$\star$ & Specht模$S^\lambda$不可约 \\
    \S2&\quad T6\;\;$\star$ & 不同分拆给出不同表示（分类完成） \\
    \S2&\quad T7\;\;$\star$ & $\dim S^\lambda = f^\lambda$ \\
    \S3.1&\quad T8\;\;$\star$ & Frobenius特征标公式 \\
    \S3.2&\quad D3.3--D3.4 & 边界带与边界带表的定义 \\
    \S3.3&\quad T9\;\;$\star$ & Murnaghan-Nakayama法则 \\
    \S3.4&\quad T10\;\;$\star$ & Frame-Robinson-Thrall钩长公式 \\
    \hline
\end{tabular}
\par}

\medskip
受限于篇幅，本文未涉及以下与本主题密切相关的重要方向，在此简注以资进一步阅读：
\begin{itemize}[nosep]
    \item \textbf{限制与诱导表示}：$S_n$的不可约表示限制到子群$S_{n-1}$时按"删去角格"规则分解，其逆过程诱导表示则由"添加方格"规则控制。这是Young图在表示论中的另一核心应用。
    \item \textbf{Frobenius特征标映射的深入理论}：本文仅在类函数空间与对称多项式环之间架设了映射$\mathcal{F}$以完成证明，$\mathcal{F}$本身作为环同构（Frobenius同构）有更丰富的结构，将$S_n$的表示环与对称函数环完全等同起来。
    \item \textbf{Littlewood-Richardson法则}：对于两个对称群的表示张量积的分解，Littlewood-Richardson法则通过"格子词"给出组合描述，是Young表理论的深远推广之一。
    \item \textbf{模表示论}：本文均基于特征$0$的常表示论。对正特征域上的对称群表示（模表示论），Young图与$p$-核、$p$-商等概念同样扮演核心角色。
\end{itemize}

综上所述，对称群的表示论之所以优美，在于它深度嫁接了群论、组合学与对称函数三个领域，使得"不可约表示"这一代数对象完全等价于"分拆"这一组合对象，而特征标的计算最终转化为Young图上的图形操作。这一理论框架深刻影响了二十世纪后期表示论、代数组合学乃至数学物理的发展。


\printindex


\medskip


\medskip
\begin{thebibliography}{99}
\bibitem{FultonHarris}
W.~Fulton, J.~Harris,
\emph{Representation Theory: A First Course},
Graduate Texts in Mathematics, Vol.~129, Springer, 1991.

\bibitem{Sagan}
B.~E.~Sagan,
\emph{The Symmetric Group: Representations, Combinatorial Algorithms, and Symmetric Functions},
Graduate Texts in Mathematics, Vol.~203, Springer, 2001.
\end{thebibliography}

\end{document}