# 讲稿.tex 修改方案（对齐 §4 框架）

## 已完成的预案

完整的改写版 `.tex` 已准备就绪，相比当前文件做了以下变更：

---

## 变更摘要

### 导言区
- 添加 `\newtheorem*{thm*}{定理}` 用于非课本编号的定理

### §一：Artin 生平 + 上节衔接（新增 ~35 行）
- Emil Artin 简介及 Wedderburn → Artin 历史
- Artin/Noether 性回顾
- 合成列存在条件：用 `thm*` + `proof` 环境

### §二：引子——群表示两个问题（新增 ~20 行）
- 明确设问：不可约表示数 = 共轭类数、维数平方和 = |G|
- 指出目标同构式，本节任务即为证明之

### §三：半单代数定义（~10 行）
- 用 `\begin{defn}`...`\end{defn}` 承载 4.1
- 将 4.2 (Maschke 定理) 压缩为一句话并入定义末尾

### §四：Wedderburn-Artin 定理（核心，~80 行）
- **保留** 4.4 反代数引理 + 完整证明（原文原样）
- **新增**反代数定义说明（"预备知识：反代数"段）
- 4.3 引理（单⇒半单）**移入** 4.5 (iv)→(i) 证明内部
- 4.5 用 `thm` + `proof`，各步标注小标题

### §五：推论展开（~120 行）
- 4.6 推论（`cor` + `proof`）
- 4.7 定理（`thm` + `proof`）——**(i)(ii)(iii) 全部保留**，含 End 函子详细推导
- 4.8 推论（`cor` + `proof`）
- **删除 4.9**（左右对称性段落）
- 4.10 唯一性定理——**保留完整证明**（`thm` + `proof`）

### §六：回到群代数（~60 行）
- 4.11 定理（`thm` + `proof`）
- 4.12 命题（`prop` + `proof`）
- **新增收束段落**——显式回扣引子两个问题

### §七：推广与展望（新增 ~30 行）
- 审视证明用到的条件（DCC + 半单性）
- Wedderburn-Artin 定理完全形态（Artin 1927），用 `thm*`
- 引出 Jacobson 根与下章内容

### 全局格式统一
- 所有 `\textbf{证}` → `\begin{proof}`，删除 `\hfill $\Box$`
- 所有 `\textbf{4.x 类型}` → `\begin{type}`...`\end{type}`
- 计数器管理：`\setcounter{thm}{0}` 在 §一和 §三处各重置一次
- 4.9 空位用 `\addtocounter{thm}{1}` 跳过
- 4.10 前 `\addtocounter{thm}{1}` 使编号对齐
- 4.11 前再 `\addtocounter{thm}{1}`

---

## 执行方式

退出计划模式后，用 `write` 工具将完整内容写入 `讲稿.tex` 即可。
完整内容约 330 行 LaTeX，已在本会话中准备好。

